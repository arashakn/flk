<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <import type="android.view.View" />
        <variable
            name="photoAlbumViewModel"
            type="com.flickr.photoalbum.PhotoAlbumViewModel" />
    </data>

<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    >

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_photos"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/tv_info"
        >

    </androidx.recyclerview.widget.RecyclerView>

    <ProgressBar
        android:id="@+id/prgBar"
        android:layout_width="50dip"
        android:layout_height="50dip"
        android:layout_centerInParent="true"
        android:visibility="@{photoAlbumViewModel.loading ? View.VISIBLE : View.GONE}"
        />
    <TextView
        android:gravity="center"
        android:id="@+id/tv_info"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_alignParentBottom="true"
        android:text="@{String.valueOf(photoAlbumViewModel.imagesList.size())"
        android:visibility="@{photoAlbumViewModel.loading ? View.GONE : View.VISIBLE}"
        />

</RelativeLayout>

</layout>